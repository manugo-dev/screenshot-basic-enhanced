// Loaded via triple-slash references rather than `compilerOptions.types` because
// @citizenfx/server is a plain package (not an @types package), and resolving it
// through typeRoots is not portable across TypeScript versions.
/// <reference types="@citizenfx/server" />
/// <reference types="node" />
