// Loaded via a triple-slash reference rather than `compilerOptions.types` because
// @citizenfx/client is a plain package (not an @types package), and resolving it
// through typeRoots is not portable across TypeScript versions.
/// <reference types="@citizenfx/client" />
